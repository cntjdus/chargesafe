# ESP32 ↔ ChargeSafe 백엔드 연결 가이드

> 도킹스테이션(ESP32)이 센서값을 서버로 보내고, 서버 판단에 따라 충전을 차단하도록 연결하는 방법

---

## 0. 전체 그림

```
   ESP32                              백엔드 서버
   ─────                              ──────────
   센서 읽기
     ↓
   자체 안전 판단 ── 위험하면 즉시 차단 (네트워크 없어도 동작)
     ↓
   5초마다 전송 ──── POST /api/ingest/readings ──→  위험 판단
                                                    DB 저장
                                                    보호자 알림
     ←──────────── { level, cutoff, fan } ────────
     ↓
   릴레이·냉각팬·부저·LED 제어
```

**중요한 원칙**: 안전 차단은 **ESP32가 스스로** 먼저 합니다.
와이파이가 끊기거나 서버가 느려도 위험 상황에서는 즉시 차단되어야 하기 때문입니다.
서버는 기록·알림·보호자 확인을 담당하는 **두 번째 방어선**입니다.

---

## 1. 준비물

| 항목 | 설명 |
|---|---|
| Arduino IDE | [arduino.cc/en/software](https://www.arduino.cc/en/software) 에서 설치 |
| ESP32 보드 패키지 | 아래 2단계에서 설치 |
| ArduinoJson 라이브러리 | 아래 2단계에서 설치 (이것 하나만 추가로 필요) |
| 기기 API 키 | 대시보드에서 기기 등록 시 **한 번만** 표시되는 `csk_...` 문자열 |
| 와이파이 | **2.4GHz** 여야 합니다 (ESP32는 5GHz를 지원하지 않습니다) |

---

## 2. Arduino IDE 설정 (처음 한 번만)

### ① ESP32 보드 패키지 설치

1. Arduino IDE → `파일` → `기본 설정`
2. **추가 보드 관리자 URL** 칸에 아래 주소를 붙여넣고 확인
   ```
   https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
   ```
3. `도구` → `보드` → `보드 관리자` → `esp32` 검색 → **esp32 by Espressif Systems** 설치

### ② ArduinoJson 라이브러리 설치

`도구` → `라이브러리 관리` → `ArduinoJson` 검색 → **ArduinoJson by Benoit Blanchon** 설치

### ③ 보드 선택

`도구` → `보드` → `esp32` → **ESP32 Dev Module** (사용 중인 보드에 맞게)
`도구` → `포트` → ESP32가 연결된 COM 포트 선택

---

## 3. 기기 등록해서 API 키 받기

ESP32가 서버에 접속하려면 **기기마다 고유한 API 키**가 필요합니다.
비밀번호 같은 역할이라, 이 키가 있어야만 데이터를 보낼 수 있습니다.

1. 대시보드 로그인 → **기기 관리** → `+ 기기 추가`
2. 시리얼 번호 입력 (예: `CS-0042`) — 기기를 구분하는 이름이므로 원하는 대로 정하면 됩니다
3. 등록하면 `csk_` 로 시작하는 키가 표시됩니다

> ⚠️ **이 키는 등록 직후 딱 한 번만 보입니다.** 창을 닫으면 다시 볼 수 없으니 바로 복사해 두세요.
> (서버에는 키 자체가 아니라 해시만 저장되기 때문입니다. 잃어버렸다면 기기를 해제하고 다시 등록하면 새 키가 발급됩니다.)

---

## 4. 펌웨어 열고 수정하기

`chargesafe_esp32` 폴더 안의 **`chargesafe_esp32.ino`** 를 더블클릭하면 Arduino IDE가 열립니다.

파일 위쪽 **① 표시 구역**만 수정하면 됩니다.

```cpp
const char* WIFI_SSID     = "와이파이_이름";
const char* WIFI_PASSWORD = "와이파이_비밀번호";

const char* SERVER_URL = "https://chargesafe-zc39.onrender.com/api/ingest/readings";

const char* API_KEY = "csk_여기에_발급받은_키를_붙여넣으세요";
```

그리고 실제 배선에 맞게 **핀 번호**를 수정하세요.

```cpp
const int PIN_TEMP    = 34;   // 온도 센서
const int PIN_CURRENT = 35;   // 전류 센서 ACS712
const int PIN_VOLTAGE = 32;   // 전압 분배 회로
const int PIN_SMOKE   = 33;   // MQ-2 연기 감지
const int PIN_RELAY   = 26;   // 충전 릴레이
const int PIN_FAN     = 27;   // 냉각팬
const int PIN_BUZZER  = 25;   // 부저
const int PIN_LED_OK  = 12;   // 정상 LED
const int PIN_LED_BAD = 13;   // 위험 LED
```

> ESP32에서 **34·35·36·39번 핀은 입력 전용**입니다. 센서(입력)에는 쓸 수 있지만
> 릴레이·LED 같은 출력에는 쓸 수 없으니 주의하세요.

---

## 5. 센서 계산식 맞추기

펌웨어에는 흔히 쓰는 부품 기준으로 계산식이 들어 있습니다.
**실제 사용하는 센서에 맞게 바꿔야 정확한 값이 나옵니다.**

| 함수 | 현재 가정한 부품 | 바꿔야 할 경우 |
|---|---|---|
| `readTemperature()` | LM35 (10mV/℃) | DS18B20을 쓴다면 `DallasTemperature` 라이브러리로 교체 |
| `readCurrent()` | ACS712-20A (2.5V 기준, 100mV/A) | 5A 모델은 185mV/A, 30A 모델은 66mV/A |
| `readVoltage()` | 전압 분배 R1=30k, R2=7.5k | 저항값이 다르면 `* 5.0` 배율 수정 |
| `readSmoke()` | MQ-2 디지털 출력 (감지 시 LOW) | 모듈에 따라 HIGH일 수 있음 |

ESP32의 아날로그 입력은 **0~4095(12비트)**, 기준 전압은 약 3.3V 입니다.

> **주의**: ESP32의 ADC는 완전히 선형이 아닙니다. 정밀도가 필요하면
> 멀티미터로 실제 값을 재서 보정 계수를 곱하는 것이 좋습니다.

---

## 6. 업로드하고 확인하기

1. ESP32를 USB로 연결
2. Arduino IDE에서 **업로드**(→ 화살표 버튼) 클릭
3. `도구` → **시리얼 모니터** 열기, 속도를 **115200** 으로 설정

정상이면 이런 로그가 나옵니다.

```
=== ChargeSafe ESP32 시작 ===
와이파이 연결 중.....
연결됨! IP: 192.168.0.25
온도 32.5℃  전류 1.20A  전압 12.40V  연기 없음  충전 중
[전송] 성공: {"level":"normal","cause":null,"session_id":"21"}
```

그다음 **대시보드를 열어** 기기 상태가 "충전 중"으로 바뀌고 센서값이 표시되는지 확인하세요.

---

## 7. 주고받는 데이터 형식

### 보내는 것 (ESP32 → 서버)

```
POST /api/ingest/readings
Content-Type: application/json
X-API-Key: csk_발급받은키
```
```json
{
  "charging": true,
  "temperature": 32.5,
  "current_a": 1.2,
  "voltage_v": 12.4,
  "smoke": false
}
```

| 필드 | 뜻 |
|---|---|
| `charging` | 지금 충전 중인지 (`true`/`false`) |
| `temperature` | 배터리 온도 (℃) |
| `current_a` | 충전 전류 (A) |
| `voltage_v` | 배터리 전압 (V) |
| `smoke` | 연기 감지 여부 |

> `charging` 이 `true` 인데 진행 중인 충전 기록이 없으면 **서버가 알아서 충전 세션을 시작**하고,
> `false` 가 오면 세션을 닫으면서 최고 온도·최대 전류를 정리합니다.
> 그래서 펌웨어는 세션을 신경 쓸 필요가 없습니다.

### 받는 것 (서버 → ESP32)

```json
{ "level": "danger", "cause": "overheat", "session_id": "21", "cutoff": true, "fan": false }
```

| 필드 | 펌웨어가 할 일 |
|---|---|
| `level` | `normal` / `caution` / `warning` / `danger` — 상태 표시 |
| `cutoff` | `true` 면 **충전 릴레이 차단** |
| `fan` | `true` 면 **냉각팬 작동** |
| `cause` | 원인 (`overheat`, `smoke`, `temp_high` 등) — 로그·화면 표시용 |

> `cutoff` 과 `fan` 은 대시보드 **설정 화면의 기기별 설정**을 반영한 결과입니다.
> 예를 들어 사용자가 "냉각팬 자동 작동"을 꺼두면 주의 단계여도 `fan: false` 가 옵니다.
>
> 아직 서버에 이 필드가 없는 버전이라면 펌웨어가 `level` 을 보고 스스로 판단하도록 되어 있으니
> 어느 쪽이든 동작합니다.

---

## 8. 서버의 위험 판단 기준

펌웨어의 자체 기준과 **같은 값으로 맞춰 두는 것**을 권합니다 (12V 계열 배터리 기준).

| 항목 | 주의 | 경고 | 위험 (자동 차단) |
|---|---|---|---|
| 온도 | 40℃ | 45℃ | **50℃** |
| 전류 | 4A 이상 | — | — |
| 전압 | — | 14.5V 이상 | — |
| 연기 | — | — | **감지 즉시** |

온도 차단 기준은 대시보드 **설정 → 온도 차단 기준**에서 기기마다 40~65℃로 바꿀 수 있습니다.

---

## 9. 문제가 생겼을 때

| 증상 | 원인과 해결 |
|---|---|
| 와이파이 연결 실패 | **5GHz 와이파이는 안 됩니다.** 2.4GHz로 연결하세요. SSID·비밀번호 오타도 확인 |
| `[전송] 401` | API 키가 틀렸거나, 대시보드에서 기기를 등록 해제한 상태입니다. 기기를 다시 등록해 새 키를 받으세요 |
| 첫 전송이 20~50초 걸림 | **정상입니다.** Render 무료 플랜은 15분간 요청이 없으면 잠들고, 첫 요청이 서버를 깨웁니다. 이후로는 빨라집니다 |
| `[전송] 실패 코드: -1` | 서버 주소 오타이거나 타임아웃입니다. `SERVER_URL` 을 확인하고, 브라우저에서 그 주소가 열리는지 보세요 |
| 센서값이 이상함 | 계산식이 실제 부품과 다릅니다. 5번 항목을 참고해 수정하세요 |
| 업로드 중 멈춤 | ESP32의 **BOOT 버튼을 누른 채로** 업로드를 시작하면 되는 보드가 있습니다 |
| 대시보드에 안 보임 | 등록한 기기와 API 키가 같은 기기인지 확인. 시리얼 모니터에 200이 찍히는데 안 보이면 대시보드를 새로고침 |

---

## 10. 로컬 PC에서 먼저 테스트하고 싶다면

배포 서버 대신 본인 PC의 백엔드로 붙이면 더 빠르게 확인할 수 있습니다.

1. PC에서 백엔드 실행
   ```bash
   cd backend
   npm run dev
   ```
2. PC의 IP 확인 — 명령 프롬프트에서 `ipconfig` → `IPv4 주소` (예: `192.168.0.10`)
3. 펌웨어의 주소를 이렇게 바꾸기
   ```cpp
   const char* SERVER_URL = "http://192.168.0.10:8000/api/ingest/readings";
   ```
4. **ESP32와 PC가 같은 와이파이**에 연결되어 있어야 합니다
5. 연결이 안 되면 PC 방화벽에서 8000번 포트를 허용하세요

---

## 11. 서버 없이 회로만 먼저 확인하려면

센서·릴레이 배선이 맞는지부터 보고 싶다면, 펌웨어에서 전송 부분만 잠시 끄면 됩니다.
`loop()` 안의 아래 줄을 주석 처리하세요.

```cpp
// sendReading(temp, current, voltage, smoke, charging);
```

시리얼 모니터에 센서값이 계속 출력되므로, 배선과 계산식을 먼저 검증할 수 있습니다.
검증이 끝나면 주석을 풀면 됩니다.
